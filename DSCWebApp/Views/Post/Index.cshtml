@model IEnumerable<Post>
@{
    ViewData["Title"] = "Posts";
}
<div class="container p-3">
    <div class="row pt-3">
        <div class="col-6">
            <h2 class="text-primary">Posts List</h2>
        </div>
        <div class="col-6 text-end">
            <a onClick=Create("/Product/QuickCreate") class="btn btn-primary text-white">
                <i class="bi bi-plus-circle-fill text-white"></i> &nbsp;Quick New Post</a>
            <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle-fill"></i> &nbsp; New Post</a>
        </div>
    </div>
</div>
<br />
<table class="table table-bordered table-striped" style="width:100%" >
    <thead>
        <tr>
            <th>
             Picture
            </th>
            <th>
              Post Title
            </th>
            <th>
                 Category
            </th>
            <th>
                Edit
            </th>
            <th>
                Delete
            </th>
            <th>
                Details
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {<tr>
            <th width=5%>
                @if(obj.PostPicture != null)
                {
                    <img id="ProductPicture" alt="profile" class="avatar rounded-circle" src="data:image/*;base64,@(Convert.ToBase64String(obj.PostPicture))" />
                }
                else
                {
                        <img id="ProductPicture" alt="profile" class="avatar rounded-circle" src="~/images/Images/Logo.jpg" />
                }
              
            </th>
            <th width=10%>
              @obj.Title
            </th>
          
           
            <th width=10%>
                @obj.Category.Name
            </th>
            <th width=5%>
                <div class="w-75 btn-group" role="group">
                    <a asp-controller="Post" asp-action="Edit" asp-route-id="@obj.Id" class="btn  btn-sm btn-primary mx-2">
                        <i class="bi bi-pencil-square"></i>Edit</a>
                </div>
            </th>
            <th width=5%>
                <div class="w-75 btn-group" role="group">
                        <a onClick=Delete("/Product/Delete/@obj.Id") class="btn  btn-sm btn-primary text-white mx-2">
                        <i class="bi bi-trash3 text-white"></i>Delete</a>
                </div>
            </th>
            <th width=5%>
                <div class="w-75 btn-group" role="group">
                    <a asp-controller="Post" asp-action="Details" asp-route-id="@obj.Id" class="btn btn-sm btn-primary mx-2">
                        <i class="bi bi-pencil-square"></i>Details</a>
                </div>
            </th>
        </tr>
        }
    </tbody>
</table>



@section Scripts{
    <script src="~/js/Product.js"></script>
}